<div class="nested-form-wrapper" data-new-record="<%= f.object.new_record? %>">
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <button type="button" data-action="nested-form#remove">Remove lesson</button>

  <%= f.hidden_field :_destroy %>
</div>
